# Local .terraform directories
**/.terraform/*

#lock file
*.lock.hcl

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# # Exclude all .tfvars files, which may contain sensitive data
# *.tfvars
# *.tfvars.json

# Ignore override files (used for local resource overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of `terraform plan`
# example: *tfplan*
